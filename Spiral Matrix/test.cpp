#include "Solution.h"

int main()
{
	Solution sl;
	vector<vector<int>> matrix(2, vector<int>(1, 0));
	matrix[0][0] = 3;
	matrix[1][0] = 2;

	sl.spiralOrder(matrix);

	char a[20];
	char b[30];

}